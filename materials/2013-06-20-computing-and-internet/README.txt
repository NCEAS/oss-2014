Some geeky presentations about computers and networks.

Built from Markdown into HTML slides using pandoc. Build with:

pandoc -i --self-contained -t s5 -s virtual-collaboration.md -o virtual-collaboration.html
